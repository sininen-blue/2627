What is the fundamental reason computers use bits (binary) rather than decimal digits?

- Bits offer better reliability and higher information density
- Bits are easier for humans to read and debug
- Bits allow direct representation of decimal numbers
- Bits require more complex hardware than decimal systems

---

In a BCD (Binary Coded Decimal) representation using 16 bits, what is the maximum number of unique combinations?

- 10,000
- 65,536
- 9,999
- 65,535

---

If a memory has cells each containing k bits, how many unique combinations can each cell hold?

- 2^k
- k^2
- 2k
- k

---

A 64-bit word contains how many bytes?

- 8 bytes
- 4 bytes
- 64 bytes
- 16 bytes

---

In little endian byte ordering, where are the low-order bits of a 32-bit integer stored?

- In byte 0 of the word
- In byte 3 of the word
- In the highest-numbered byte
- Equally across all bytes of the word

---

Why does a 500 Mbps internet connection not download a 5 GB file in 80 seconds?

- Networking is advertised in bits per second, so 500 Mbps transfers only 62.5 MB/s
- Network overhead reduces the effective speed by more than half
- File sizes in GB are measured in decimal while networking uses binary
- The 500 Mbps rating is a theoretical maximum that real connections never reach

---

What is the minimum Hamming distance required to detect d single-bit errors?

- d + 1
- d
- 2d + 1
- 2d

---

In a code with four valid codewords, each a minimum Hamming distance of 5 apart from the others, what is the maximum number of bit errors that can be corrected?

- 2
- 5
- 3
- 4

---

In Hamming's algorithm for a 16-bit memory word, which bit positions become parity bits?

- 1, 2, 4, 8, and 16
- 1, 3, 5, 7, and 9
- 2, 4, 8, 16, and 32
- 0, 1, 2, 4, and 8

---

What modern approach has largely solved the byte ordering problem between different machines?

- Text-based serialization like JSON with UTF-8 encoding
- Requiring all machines to use big endian ordering
- Using larger word sizes to eliminate byte ordering issues
- Storing all integers as 64-bit values regardless of architecture
