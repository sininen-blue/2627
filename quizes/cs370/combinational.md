What determines the output of a combinational circuit at any instant?

- The current input values
- The past state of the circuit
- The sequence of previous inputs
- The number of memory elements

---

How does a combinational circuit respond to a given set of inputs?

- It always produces the same outputs
- It depends on its internal history
- It stores the input for later use
- It alternates between two results

---

What is a distinguishing feature of a sequential circuit compared to a combinational one?

- It has memory and depends on past state
- It responds only to current inputs
- It cannot be built from logic gates
- It produces a single fixed output

---

Which of the following is an example of a sequential circuit?

- A flip-flop
- A multiplexer
- An adder
- A comparator

---

Which of the following is an example of a combinational circuit?

- A multiplexer
- A flip-flop
- A register
- A memory cell

---

How many data inputs does a multiplexer with n control pins have?

- 2^n data inputs
- n data inputs
- 2n data inputs
- n^2 data inputs

---

What is the purpose of a multiplexer's control pins?

- They select which data input reaches the output
- They supply the power to the circuit
- They store the output value in memory
- They amplify the selected input signal

---

With 3 control pins, how many data inputs can a multiplexer choose between?

- 8 data inputs
- 3 data inputs
- 6 data inputs
- 9 data inputs

---

What does a demultiplexer do?

- Routes a single input to one of many outputs
- Combines many inputs into a single output
- Compares two inputs for equality
- Selects one of many inputs to forward

---

How many outputs does a demultiplexer with n control pins have?

- 2^n outputs
- n outputs
- 2n outputs
- n^2 outputs

---

How is a multiplexer like a controllable switch?

- Its control value acts as an address for an input
- Its output alternates on and off
- It remembers the last selected input
- It routes one input to many outputs

---

What does a decoder do with its n-bit input?

- It sets exactly one of its output lines to 1
- It forwards the input to a selected output
- It compares the input against a constant
- It stores the input in its output lines

---

Why does only one memory chip get enabled in the eight-chip decoder example?

- The decoder output is one-hot
- The chips share a single wire
- The chips check their own addresses
- The address lines are all equal

---

How does a decoder scale as input bits increase?

- n bits produce 2^n output lines
- n bits produce n output lines
- n bits produce 2n output lines
- n bits produce one output line

---

What does a comparator's XOR gates detect about two input bits?

- Whether the two bits are equal
- Whether the first bit is larger
- Whether the sum is even
- Whether either bit is zero

---

What does the final NOR gate do in the comparator?

- Reverses the sense so 1 means equal
- Adds a delay to the output
- Doubles the number of outputs
- Stores the comparison result

---

How does a combinational circuit respond to a repeated set of inputs?

- It produces the same output every time
- It produces a different output each time
- It depends on how many times it ran
- It requires a reset before each run
