In the box model for variables, what is the variable?

- The box that holds a value
- The blob of stuff inside
- The label on the code
- The program itself

---

In the box model for variables, what is the value?

- The stuff inside the box
- The label on the box
- The name of the box
- The program that uses it

---

What happens when you write name = 20?

- You make a box, label it, and put 20 inside
- You copy the number 20 into every box
- You replace the label with the number
- You delete any box called name

---

When Python sees 5 < name, what does it do with name?

- Replaces it with the value in the box
- Compares the label alphabetically
- Creates a new box called name
- Erases the box and returns nothing

---

What is the purpose of a configuration bundle?

- Setting variables at the top so tweaking is easier
- Counting how many times a key is pressed
- Storing the current state of the game
- Remembering the highest score

---


What is a counter variable used for?

- Counting things, like going up by 1 each time
- Switching between game states
- Storing the game's settings
- Comparing two records

---

When does a countdown timer stop?

- When it hits a target value
- When it reaches the top of the script
- When the score changes
- When a flag is set to False

---

What does a flag variable do?

- Switches between values
- Counts how many times something happens
- Holds the best score seen so far
- Sets the speed at the top of the script

---

What does a flag replace in your code?

- A tangled mess of if blocks
- A configurable bundle of settings
- A loop that counts down
- A temporary swap variable

---

Which are examples of discrete states for a flag?

- "playing", "paused", "game over", "menu"
- speed, jumpForce, mass, gravity
- score, highScore, timeLeft, temp
- x, y, a, b

---

How does a max tracker keep the highest score?

- It only updates when the new score beats the record
- It adds every new score together
- It resets back to zero each time
- It switches between two fixed values

---

What does highScore = 0 followed by if score > highScore do?

- Only updates the record when it's beaten
- Updates highScore on every change
- Doubles highScore each frame
- Stops once highScore equals score

---

Which code swaps x and y using a temporary variable?

- temp = x; x = y; y = temp
- x = y; y = x
- temp = y; y = x; x = y
- x = temp; temp = y; y = x

---

What is the one-line Python shortcut for swapping x and y?

- x, y = y, x
- x = y = x
- swap(x, y)
- temp = x, y = temp
