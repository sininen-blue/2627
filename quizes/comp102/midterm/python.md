What does it mean for a programming language to be "high level"?

- It is removed from how the computer actually works
- It is the newest language on the market
- It only works on high-powered computers
- It is built directly from machine code

---

Which two languages are named as examples of high-level languages?

- Python and Scratch
- C and Assembly
- Shell and Binary
- C and Python

---

Why do high-level languages run slower than the computer's own actions?

- They are abstracted away from the computer's workings
- They contain far fewer features
- They only model one specific task
- They skip all convenience features

---

Scratch prioritizes which of the following?

- Human understanding over ease of computing
- Ease of computing over human understanding
- Speed over everything else
- The exact logic of the machine

---

A student edits one line of their Python script and runs it again immediately. How does Python process the file on the second run?

- It reads and interprets the file from scratch, line by line
- It detects the changed line and recompiles only that part
- It reuses a cached executable produced during the first run
- It asks the user to confirm the change before running

---

Which combination describes Python as a programming language?

- High level, interpreted, object oriented, and general purpose
- Low level, compiled, procedural, and domain specific
- High level, compiled, functional, and domain specific
- Low level, interpreted, procedural, and general purpose

---

What does Python emphasize?

- Readability and developer ease of use
- Raw speed and minimal features
- Memory efficiency and tiny code
- Hardware control and performance

---

Which languages are usually considered high level in common practice?

- Python, Ruby, and Lua
- Assembly, C, and Fortran
- SQL, HTML, and MATLAB
- C, Java, and Assembly

---

Why is the "level" of a language not a formal definition?

- It depends on your field and the context
- It depends on when the language was made
- It depends on how long your code is
- It depends on the speed of your computer

---

What does it mean for a language to be interpreted?

- It is slower but has no separate compile step
- It is faster but requires manual compilation
- It must be turned into an executable first
- It cannot work with variables at all

---

In object-oriented languages, most things have attributes and methods. What do these represent?

- Things they are and things they can do
- Inputs they take and outputs they give
- Names they have and values they hold
- Types they use and errors they raise

---

How do general purpose and domain specific languages differ?

- domain specific solves one kind of problem
- domain specific solves any kind of problems
- General purpose is compiled
- General purpose is interpreted

---

Which language is domain specific for querying databases?

- SQL
- Java
- Ruby
- Lua

---

What does a REPL let you do?

- Type Python code and see results immediately
- Compile code into a finished executable
- Convert code into another language
- Run code directly on the hardware

---

How does a compiled language run, compared to Python?

- Code is transformed into an executable the computer runs
- Code is fed line by line into a virtual machine
- Code never needs to be translated at all
- Code is stored as plain text forever

---

How does Python run your code?

- A virtual machine reads and converts it line by line
- A compiler transforms it into an executable first
- The hardware runs it without any translation
- A shell script executes it directly

---

What do filename extensions like .py and .txt do?

- Tell the operating system what program should open the file
- Change what is stored inside the file
- Determine which language the file uses
- Make the file run faster

---

Can you run a Python script saved as myprogram.txt?

- Yes 
- No 
- Only after renaming it to a .py extension
- Only if it runs inside a virtual machine

---

What is an expression?

- Any piece of code that produces a value
- Any line that starts with a hash
- A named box that stores data
- A command that repeats code

---

How does Python evaluate an expression like (x + 3) * 4?

- Inside out, evaluating the innermost parts first
- Left to right, ignoring the parentheses
- Right to left, starting with the outside
- In whatever order it feels like

---

What does the modulo operator (%) do?

- Gives the remainder of a division
- Gives the full result of a division
- Rounds a decimal to a whole number
- Checks if two values are equal

---

What does 10 // 3 evaluate to?

- 3
- 1
- 4
- 3.33

---

What does print() return when you use it?

- None, Python's value for no result
- The text it wrote to the screen
- The integer zero by default
- A True or False value

---

What happens when you run "hello" + 5?

- A TypeError — Python cannot mix strings and ints with +
- The string 'hello5', since Python silently converts the integer
- The integer 5, since the string is discarded by the operator
- The string 'hellohellohellohellohello', since + acts as a repetition operator

---

What does an if statement let your code do?

- Make decisions based on True or False conditions
- Repeat a block a set number of times
- Store values inside named boxes
- Convert between different data types
