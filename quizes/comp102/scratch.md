In a flowchart, the oval shaped Start/End symbol represents where a program begins. What Scratch block fills the same role?

- An Event hat block like "when flag clicked"
- A Control block like "wait 1 seconds"
- A Motion block like "move 10 steps"
- A Variables block like "set score to 0"

---

A flowchart rectangle represents a process or action. What kind of Scratch block replaces it?

- Motion, Looks, or Variables blocks performing an action
- Event blocks that trigger scripts to start running
- Control blocks that manage loops and conditionals
- Sensing blocks that detect keyboard inputs

---

A flowchart diamond represents a Yes/No decision. How is this represented in Scratch?

- An "if then" block with a hexagon-shaped condition slot
- A "repeat" block wrapped around other action blocks
- A "when key pressed" event block for keyboard input
- A "change score by 1" variable update block

---

A flowchart loop repeats a sequence of steps. Which Scratch block corresponds to this pattern?

- A C-shaped Control block that wraps around other blocks
- A hat block that sits at the top of a script stack
- A stack block with a notch on both top and bottom
- A reporter block that returns a numeric value

---

Event blocks like "when flag clicked" have a rounded top with no notch above them. What does this shape communicate?

- Nothing can be placed above them, so they always start a script
- They can be placed anywhere in a stack of connected blocks
- They are designed to fit inside C-shaped loop blocks
- They produce a value that snaps into hexagonal slots

---

An "if then" block has a hexagon-shaped slot for its condition. What kind of value does this slot expect?

- A true or false Boolean value
- A number like 10 or 100
- A text string like "hello"
- A sprite name from the project

---

A "repeat" block wraps around other blocks with a C-shaped opening. What is the purpose of this shape?

- To contain the blocks that should be repeated inside it
- To mark the top of a script like a hat block does
- To indicate that the block produces a numeric value
- To show that the block connects to the stage directly

---

Ordinary stack blocks have a notch on top and a tab on the bottom. What does this design allow?

- Blocks to snap together in a vertical sequence
- Other blocks to be placed above them as hat blocks
- The blocks to fit inside C-shaped control structures
- The blocks to report values to Boolean slots

---

A student wants a sprite to walk 10 steps when the green flag is clicked. Which block category provides both the trigger and the movement?

- Events for the trigger and Motion for the movement
- Control for the trigger and Motion for the movement
- Events for the trigger and Looks for the movement
- Control for the trigger and Sensing for the movement

---

Which category contains blocks that change a sprite's costume or show speech bubbles?

- Looks
- Motion
- Events
- Control

---

A student needs to detect whether the sprite is touching the edge of the stage. Which block category should they use?

- Sensing
- Motion
- Control
- Events

---

Which category contains the block that runs a script when the space key is pressed?

- Events
- Control
- Sensing
- Motion

---

A student writes a script for Sprite A that moves it 10 steps when the flag is clicked. When the flag is clicked, will Sprite B run that same script?

- No, each sprite has its own scripts and only runs its own code
- Yes, all sprites share the same canvas and scripts on the stage
- No, Scratch does not allow the green flag to start any scripts
- Yes, but only if Sprite B has the same costume as Sprite A

---

What is a sprite in Scratch?

- An actor on the stage with its own scripts, costumes, and sounds
- A block category used for detecting mouse clicks and key presses
- The background image displayed behind all game action
- A C-shaped block that wraps around other logic blocks

---

What does the "repeat 10" block do when the program runs?

- It runs the blocks inside it exactly ten times in a row
- It repeats blocks forever until the user manually stops the program
- It checks a condition and runs blocks once if the condition is true
- It waits ten seconds before running the blocks inside it

---

How does "repeat until" differ from "forever" in Scratch?

- "repeat until" stops when its condition is met, while "forever" never stops
- "repeat until" runs a fixed number of times, while "forever" does not
- "repeat until" requires a wait block inside it, while "forever" does not
- "repeat until" can only be used inside a "forever" block

---

A student needs to give a score variable a starting value of zero at the beginning of the game. Which block should they use?

- "set score to 0"
- "change score by 0"
- "show variable score"
- "score" reporter block

---

What does the "change score by 1" block do when it executes?

- It adds one to the current value of the score variable
- It sets the score variable back to a value of one
- It removes one from the current value of the score variable
- It creates a new variable named score with value one

---

A script starts with "when flag clicked", then "move 10 steps", then "if touching edge then" with a "turn 15 degrees" inside it, then loops back to "move 10 steps". This structure describes which flowchart pattern?

- A loop with a decision inside it
- A simple sequence of actions
- A decision with no repetition
- A start and end with nothing between

---

Every script in Scratch must begin with a specific kind of block. What type of block is required at the top?

- A hat block with a rounded top
- A stack block with notches
- A C-shaped control block
- A Boolean hexagon block
